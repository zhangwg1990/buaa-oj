#include<stdio.h>
#include<stdlib.h>
void replace(char s[], int start, int end, char str[])
{
	int len = 0;
	while (NULL != s[len]) len++;
	if (start > )
}
int main()
{
	int n;
	char s[1024], str[1024];
	while (NULL != gets(s)) {
		scanf("%d", &n);
		while (n--) {

		}
	}
	system("pause");
	return 0;
}