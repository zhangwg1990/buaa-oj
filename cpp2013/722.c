#include <stdio.h>

int main() {
    int x, y;
    int n;
    int valley;
    int i;
    int a[100], b[100], r[100], h[100];
    char c[100];
    while (EOF != scanf("%d %d", &x, &y)) {
        scanf("%d", &n);
        for (i = 0; i < n; i++) {
            scanf("%d %d %d %d %c", a + i, b + i, r + i, h + i, c + i);
        }
        for (i = 0; i < n; i++)
    }
}
